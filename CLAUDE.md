Read `AGENTS.md` first: it states the constitution of this repository, the
contracts that must not drift and the procedure for adding a feature. Use
the `maelys-cli-framework` skill for any change to the public surface, and
run `make check` before reporting a change as done.
